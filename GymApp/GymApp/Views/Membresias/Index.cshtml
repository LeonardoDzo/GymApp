@model IEnumerable<GymApp.Models.Membresias>

@{
    ViewBag.Title = "Membresías ";
}
<LINK href="~/Content/Chalkboard.css" rel="stylesheet" type="text/css">

<h2>Membresías </h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
@*<table class="table table-bordered table-condensed table-hover" >
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Costo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Turno)
        </th>
        <th></th>
    </tr>*@

@foreach (var item in Model)
{<div class="col-lg-2 col-md-3">
        <div class="service-wrapper" style="text-align:center">
            <div id="pricing-table" class="clear">
                <div class="plan" id="most-popular">
                    <h3>@Html.DisplayFor(modelItem => item.Nombre)<span>@Html.DisplayFor(modelItem => item.Costo)</span></h3>
                    <a class="signup" href="@Url.Action("Edit", "Membresias", new { id = item.id })" class="btn">
                        <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
                    </a>
                    <ul>
                        <li><b>Turno </b>@Html.DisplayFor(modelItem => item.Turno) <br /></li>
                        <li>
                            <b>
                                <a href="@Url.Action("Details", "Membresias", new { id = item.id })" class="btn">
                                    <span class="glyphicon glyphicon-list-alt" aria-hidden="true"></span>
                                </a>

                                <a href="@Url.Action("Delete", "Membresias", new { id = item.id })" class="btn">
                                    <span class="glyphicon glyphicon-remove" aria-hidden="true"></span>
                                </a>
                        </li>

                    </ul>
                </div>
            </div>
        </div>
    </div>
            @*<div class="col-lg-3 col-md-6">
                <div class="service-wrapper" style="text-align:center">
                    <img src="https://66.media.tumblr.com/avatar_f6db0ccd692c_128.png" /> <br />
                    @Html.DisplayFor(modelItem => item.Nombre) <br />
                   @Html.DisplayFor(modelItem => item.Costo) <br />
                   @Html.DisplayFor(modelItem => item.Turno) <br />

                    <a href="@Url.Action("Edit", "Membresias", new { id = item.id })" class="btn">
                        <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
                    </a>

                    <a href="@Url.Action("Details", "Membresias", new { id = item.id })" class="btn">
                        <span class="glyphicon glyphicon-list-alt" aria-hidden="true"></span>
                    </a>

                    <a href="@Url.Action("Delete", "Membresias", new { id = item.id })" class="btn">
                        <span class="glyphicon glyphicon-remove" aria-hidden="true"></span>
                    </a>*@

             @*@Html.ActionLink("Seleccion", "Seleccion", new { id = item.idProducto }, new { @class = "btn btn-primary" })*@

            @*</div>
                </div>*@
}
@*<tr>
    <td>
        @Html.DisplayFor(modelItem => item.Nombre)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Costo)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Turno)
    </td>
    <td>
        <a href="@Url.Action("Edit", "Membresias",new { id=item.id })" class="btn">
            <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
        </a>

        <a href="@Url.Action("Details", "Membresias",new { id=item.id })" class="btn">
            <span class="glyphicon glyphicon-list-alt" aria-hidden="true"></span>
        </a>

        <a href="@Url.Action("Delete", "Membresias",new { id=item.id })" class="btn">
            <span class="glyphicon glyphicon-remove" aria-hidden="true"></span>
        </a>*@
@*@Html.ActionLink("Edit", "Edit", new { id=item.id }) |
    @Html.ActionLink("Details", "Details", new { id=item.id }) |
    @Html.ActionLink("Delete", "Delete", new { id=item.id })*@
@*</td>
        </tr>
    }*@

@*</table>*@
